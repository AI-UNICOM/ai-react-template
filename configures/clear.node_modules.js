const fs = require('fs');
const rimraf = require('rimraf');
const DIR = require('./path.config');
console.log(DIR.NODEMODULES);
rimraf(DIR.NODEMODULES, fs, function cb() {
  console.log('node_modules目录已清空');
});